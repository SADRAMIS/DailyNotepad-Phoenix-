# Интеграция AI (OpenAI) в проект DailyNotes

## 📋 Обзор

В проект интегрирован OpenAI API для предоставления AI-функций при работе с заметками. Это позволяет:

- **Генерация идей** для заметок на основе темы
- **Автодополнение текста** при написании заметок
- **Анализ и резюме** существующих заметок
- **Генерация заголовков** на основе содержания
- **Улучшение текста** (грамматика, стиль, читаемость)
- **Генерация задач** на основе заметок

## 🏗️ Архитектура

```
┌─────────────┐
│  Web UI     │
│  (Forms)    │
└──────┬──────┘
       │
       ▼
┌─────────────┐      ┌──────────────┐      ┌─────────────┐
│AIController │─────▶│  AIService   │─────▶│  OpenAI API  │
│  (REST)     │      │              │      │             │
└─────────────┘      └──────────────┘      └─────────────┘
```

## 📦 Что было добавлено

### 1. Конфигурация (`OpenAIConfig.java`)
- Настройка WebClient для HTTP запросов к OpenAI API
- Проверка наличия API ключа

### 2. DTO классы
- `AIRequest` - для запросов к AI
- `AIResponse` - для ответов от AI

### 3. AI Сервис (`AIService.java`)
Методы:
- `generateIdeas(String topic)` - генерация идей
- `autocompleteText(String partialText)` - автодополнение
- `summarizeNote(String noteContent)` - резюме заметки
- `generateTitle(String noteContent)` - генерация заголовка
- `improveText(String noteContent)` - улучшение текста
- `generateTasks(String noteContent)` - генерация задач

### 4. REST API (`AIController.java`)
Endpoints:
- `POST /api/ai/ideas` - генерация идей
- `POST /api/ai/autocomplete` - автодополнение
- `POST /api/ai/summarize` - резюме
- `POST /api/ai/title` - генерация заголовка
- `POST /api/ai/improve` - улучшение текста
- `POST /api/ai/tasks` - генерация задач
- `GET /api/ai/notes/{id}/summarize` - резюме заметки по ID
- `GET /api/ai/notes/{id}/title` - заголовок для заметки по ID

### 5. Веб-интерфейс
- Кнопки AI в форме создания заметок (`noteForm.html`)
- Кнопки AI в форме редактирования (`editForm.html`)
- JavaScript для асинхронных запросов к AI API

## ⚙️ Настройка

### 1. Получение API ключа OpenAI

1. Зарегистрируйтесь на https://platform.openai.com
2. Перейдите в раздел API Keys: https://platform.openai.com/api-keys
3. Создайте новый API ключ
4. Скопируйте ключ (он показывается только один раз!)

### 2. Настройка API ключа

**Вариант 1: Переменная окружения (рекомендуется)**
```bash
# Windows PowerShell
$env:OPENAI_API_KEY="your-api-key-here"

# Windows CMD
set OPENAI_API_KEY=your-api-key-here

# Linux/Mac
export OPENAI_API_KEY=your-api-key-here
```

**Вариант 2: В application.properties (не рекомендуется для продакшена)**
```properties
openai.api.key=your-api-key-here
```

### 3. Настройка модели и параметров

В `application.properties`:
```properties
# Модель OpenAI (по умолчанию: gpt-3.5-turbo)
openai.model=gpt-3.5-turbo

# Максимальное количество токенов в ответе
openai.max-tokens=500

# URL API (обычно не нужно менять)
openai.api.url=https://api.openai.com/v1
```

## 🚀 Использование

### Через веб-интерфейс

1. **Создание заметки** (`/notes/new`):
   - Кнопка "🤖 Сгенерировать заголовок" - создает заголовок на основе содержания
   - Кнопка "💡 Идеи для заметки" - предлагает идеи на основе заголовка
   - Кнопка "✍️ Автодополнение" - продолжает начатый текст
   - Кнопка "✨ Улучшить текст" - улучшает грамматику и стиль
   - Кнопка "📋 Сгенерировать задачи" - создает задачи на основе заметки

2. **Редактирование заметки** (`/notes/{id}/edit`):
   - Те же функции, плюс:
   - Кнопка "📝 Резюме" - создает краткое резюме заметки

### Через REST API

Пример запроса:
```bash
# Генерация идей
curl -X POST http://localhost:8080/api/ai/ideas \
  -H "Content-Type: application/json" \
  -d '{"prompt": "продуктивность"}'

# Автодополнение
curl -X POST http://localhost:8080/api/ai/autocomplete \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Сегодня я планирую"}'

# Резюме заметки по ID
curl http://localhost:8080/api/ai/notes/1/summarize
```

## 💰 Стоимость

OpenAI API платный. Примерные цены (на момент написания):
- GPT-3.5-turbo: ~$0.002 за 1K токенов
- GPT-4: значительно дороже

**Рекомендации:**
- Используйте GPT-3.5-turbo для большинства задач (быстрее и дешевле)
- Установите лимиты на использование в продакшене
- Мониторьте расходы через OpenAI Dashboard

## 🔒 Безопасность

1. **Никогда не коммитьте API ключи в Git!**
   - Используйте переменные окружения
   - Добавьте `application.properties` с ключами в `.gitignore` (если используете)

2. **Ограничьте доступ к API endpoints** в продакшене:
   - Добавьте аутентификацию
   - Используйте rate limiting

3. **Валидация входных данных:**
   - Ограничьте длину промптов
   - Проверяйте содержимое перед отправкой в AI

## 🐛 Решение проблем

### Ошибка: "OpenAI API ключ не настроен"
- Проверьте, что переменная окружения `OPENAI_API_KEY` установлена
- Или установите `openai.api.key` в `application.properties`
- Перезапустите приложение

### Ошибка: "Ошибка при обращении к AI"
- Проверьте интернет-соединение
- Убедитесь, что API ключ валиден
- Проверьте баланс на аккаунте OpenAI
- Посмотрите логи приложения для деталей

### Медленные ответы
- Уменьшите `max-tokens` в настройках
- Используйте более быструю модель (gpt-3.5-turbo вместо gpt-4)
- Добавьте кэширование для часто используемых запросов

## 📚 Дополнительные ресурсы

- [OpenAI API Documentation](https://platform.openai.com/docs)
- [OpenAI Pricing](https://openai.com/pricing)
- [Spring WebClient Documentation](https://docs.spring.io/spring-framework/reference/web/webflux-webclient.html)

